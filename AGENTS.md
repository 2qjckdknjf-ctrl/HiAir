## Learned User Preferences
- User prefers autonomous execution with minimal back-and-forth; for large work, audit then plan then implement in phases without mid-task confirmation unless blocked, and continue until major tasks are complete.
- User often prefers Russian for task instructions and progress communication.
- User values direct, action-oriented progress over extended planning-only responses.
- User frequently asks for deep end-to-end truth-based audits with a single consistent final status, and full hardening/closure rather than partial fixes.
- User prioritizes high-quality, polished outcomes and is comfortable trading speed for quality.
- User expects Supabase project provisioning, migrations, and server-side secrets to be handled via Supabase MCP/server automation rather than manual dashboard or local PAT flows when possible.
- User expects production infrastructure on **hiair.io** via Cloudflare (Pages, Worker proxy, Containers for API)—not Fly.io or other ad-hoc host proposals unless explicitly requested.
- For mobile login/auth blockers, user expects deep production fixes across iOS, `api.hiair.io`, and Supabase—not superficial UI-only changes.
- For brand or visual redesign work, keep scope to design tokens, components, and assets only; integrate supplied mockups or publish packs for orb, icons, and launch assets without changing backend, API, auth, data models, risk engine, or business flows, and keep UI components presentation-only.
- For subscription and store readiness, use an honest status ladder (ARCHITECTURE READY → STORE SANDBOX READY → PRODUCTION READY); never claim STORE SANDBOX READY or physical-device certification without real on-device verification; Apple/Google purchase verification must be cryptographically fail-closed in live/production (no silent stub fallback).
- For health/wearable features, require explicit user consent, store only daily/hourly aggregates (no raw streams), use wellness-only language (no diagnoses or emergency alerts), keep the app fully functional when health permissions are denied, and for iOS HealthKit blockers fix Apple Developer Portal App ID capability, provisioning/signing, Info.plist purpose strings, and real `requestAuthorization` flow—not Settings deep links or UI-only changes; revoke/delete must be local-first fail-closed (block uploads immediately), account-bound pending remote cleanup must survive logout/account switch, and UI must not claim full remote deletion on partial failure.
- For first-10-users/beta readiness, treat it as a quality-hardening sprint (no new features): find → fix → verify → confirm nothing broke before moving on, leave no TODO/FIXME/HACK/dead code/duplication, and give one honest verdict (READY FOR FIRST 10 USERS vs NOT READY); follow P0 ordering backend live data → deploy + production smoke on `api.hiair.io` → device QA → push → internal testing (never push or device-QA before live data is deployed and verified), production must never show mock/fake data (mock only in preview/debug/tests), and on-device UX must feel snappy (fast real locality name, responsive HealthKit connect, prompt Premium reflection after purchase).

## Learned Workspace Facts
- Main project workspace is `/Users/alex/Projects/HIAir` (iOS/Android apps + backend/services); GitHub repo `2qjckdknjf-ctrl/HiAir` (prefer `hiair` remote or SSH for large binary pushes when HTTPS fails); product supports multilingual UX/content across Russian, English, Spanish, Italian, French; local dev is an arm64 Mac without Rosetta/homebrew/uv—pre-existing `.venv`, Docker CLI, and `gh` are x86_64 and won't run, so backend tests run via a standalone arm64 CPython 3.12 in gitignored `.tools/py/` (~235 passing, ~74% coverage); Android builds use JDK 17 (JBR at `/Users/alex/Library/Java/JavaVirtualMachines/jbr-17.0.14/Contents/Home`); Xcode 26.6 + iOS 26 simulator, Android SDK, and a standalone `xcodegen` binary are available; leave user untracked paths like `.tools/`, ASC screenshots, and local subscription diagnostic docs untouched unless explicitly asked to commit them.
- Backend is Python/FastAPI under `backend/` (risk engine, ingestion, notifications, privacy orchestration); production data/auth use Supabase PostgreSQL with RLS and Supabase Auth, with schema migrations in `backend/sql/`.
- Production Supabase project is named `hiair-prod` (region `eu-central-1`); operator docs for Supabase/auth live under `docs/_operator/`; Supabase Management API changes need an account PAT (`sbp_...`), not the project `service_role` JWT.
- Release closure is validated with `scripts/release/hiair_final_gate.sh` (including `--strict-external` for owner/legal readiness); integrity hardening status lives in `docs/audit/P0_INTEGRITY_SPRINT_REPORT.md`.
- Brand source of truth is under `docs/brand/` (HiAir Orb / Aurora Calm; UI and store copy spell **HiAir**; tagline **Breathe better. Live better.**); iOS tokens/components live under `mobile/ios/HiAir/DesignSystem/`, Android under `mobile/android/app/src/main/java/com/hiair/ui/design/`, store graphics under `docs/brand/store-assets/`; iOS in-app orb uses `.renderingMode(.original)` with full opaque `AppIcon.appiconset`; Android launcher uses `@mipmap/ic_launcher` adaptive icons and `imageTintList = null` on orb `ImageView`s.
- Production web/API domain is **hiair.io**: marketing/legal at `https://hiair.io` (landing + waitlist `POST /api/waitlist`, migration `015_waitlist_signups.sql`), API at `https://api.hiair.io`; static site via Cloudflare Pages (`web/`, project `hiair-web`) plus Worker proxy (`infra/cloudflare/hiair-io-proxy/`); FastAPI on Cloudflare Containers (`infra/cloudflare/hiair-api/`); deploy via `scripts/ops/connect_hiair_io.sh` and `scripts/ops/deploy_hiair_api_cloudflare.sh`. iOS/Android bundle IDs remain `com.hiair.app` / `com.hiair` (not the web domain).
- TestFlight/production auth: iOS email login uses backend bridge `POST /api/auth/supabase/session` on `https://api.hiair.io` (legacy JWT `SUPABASE_SERVICE_ROLE_KEY`/`SUPABASE_ANON_KEY`, not `sb_secret_*`; sync via `backend/scripts/sync_supabase_secrets.py` + deploy); the session bridge must be password-grant only—never admin-create users, auto-confirm email, or turn unknown-email login into signup; native Sign in with Apple via `AuthenticationServices` and Supabase provider setup in `scripts/ops/configure_supabase_apple_provider.py` (Services ID `com.hiair.app.auth`, OAuth redirect `hiair://auth/callback`); operator runbook `docs/_operator/supabase-auth-dashboard-setup.md`.
- Subscriptions and iOS/Android release: entitlements in `backend/app/services/entitlement_service.py` and `backend/sql/012_subscriptions_entitlements.sql` + `013_supabase_entitlements_auth_user_fk.sql` (FK → `auth.users`); product IDs `com.hiair.premium.monthly`/`yearly` (iOS), `hiair_premium_monthly`/`yearly` (Android); Apple and Google live verifiers in `subscription_store.py` are fail-closed (JWS/signature and required fields; no `alg=none`, no synthesized expiry/transaction IDs); production deploy rejects stub verifiers and requires `APPLE_STORE_ENVIRONMENT`/`APPLE_APP_APPLE_ID` plus Google Play service-account config; docs under `docs/subscriptions/`; honest status **ARCHITECTURE READY** until ASC/Play products, paywall evidence, and real on-device sandbox purchases verify; TestFlight via Xcode Cloud (`mobile/ios/ci_scripts/ci_post_clone.sh`, Xcode 26/iOS 26 SDK—local older Xcode uploads rejected by ASC) and `mobile/ios/scripts/upload_ipa_testflight_api.sh`; App Store ops scripts `scripts/ops/check_app_store_iap.py`, `bootstrap_app_store_subscriptions.py`; App Store Connect API keys gitignored in `backend/.secrets/`.
- Backend AI/LLM lives in `backend/app/services/ai_explanation_service.py` (OpenAI httpx, fallback, guardrails, observability); `OPENAI_*` secrets belong in gitignored `backend/.env.local` (loaded by `settings.py`), not `.env.example`; live promotion gate is `backend/scripts/check_ai_connection.py --require-live`; remote gate is `scripts/release/post_deploy_api_smoke.py --require-live-ai` on `https://api.hiair.io`; audit report at `docs/audit/AI_LIVE_CONNECTION_AUDIT.md`; production deploy orchestrated by `.github/workflows/backend-deploy-production.yml` (DB/AI gate + optional Cloudflare Containers).
- First-10-users P0 hardening on branch `release/mega-sprint-2-first-10-users`: backend env resolver `resolve_environment_snapshot()` in `air_environment_service.py` does live → cached → sample (Open-Meteo default `WEATHER_API_PROVIDER`/`AQI_API_PROVIDER=openmeteo`, no API keys; 15-min geo-hash cache; source labeled Live/Cached/Sample in iOS/Android air metrics) and all consumers (`/api/air/current-risk`, `/day-plan`, `/recommendations`, `/api/dashboard/overview`, `/api/planner/daily`, morning briefing) use it; shared scoring in `backend/app/services/air_score.py` (`RISK_LEVEL_TO_SCORE` 20/45/70/90); Android dashboard is a live state machine (Initial/Loading/Success/Empty/Error/Offline + Retry) with auto profile bootstrap via `/api/profiles` (list→create, `profileId` persisted in `SessionStore`, cleared on logout/expiry) and no client-side mock; `ProductAnalytics` is an OS-logger layer with no PII (`dashboard_loading`/`loaded`/`failed`/`retry`).
- Wearable Activity v1: migration `backend/sql/014_wearable_activity.sql` (consent + daily/hourly summaries, RLS); API `/api/v1/wearables/*`; `personalLoadScore` in `backend/app/services/personal_load_engine.py` integrated into air risk; iOS HealthKit (App ID **HealthKit** capability in Apple Developer Portal, entitlements in `project.yml`, `NSHealthShareUsageDescription` + `NSHealthUpdateUsageDescription`, always call `requestAuthorization` on connect; authorization/collection timeouts must return without waiting forever on hung HK callbacks; background sync uses generation-guarded single-flight), Android Health Connect (compile/target SDK 35; account-bound pending remote cleanup tombstones survive logout and never run under another user's token); validation `scripts/ops/validate_ios_healthkit_ipa.sh`; spec `docs/feat-wearable-activity-insights-spec.md`; audits under `docs/audit/WEARABLE_ACTIVITY_*` and `docs/audit/P0_INTEGRITY_SPRINT_REPORT.md`; CI skips migration 014 when Supabase auth schema is absent.
- HiAir 1.1 Forecast Truth lives on `feat/hiair-1.1-forecast-truth` in worktree `/Users/alex/Projects/HIAir-feat-hiair-1.1-forecast-truth` (do not land on `main` until asked): `backend/app/services/forecast/` supplies Open-Meteo hourly weather+air; planner/safe windows use real timestamped points or `forecastAvailable=false`; UV/PM10/wind are never inferred; missing metrics stay null (never zero-filled); static gate `scripts/release/check_forecast_integrity.py`; production smoke `scripts/release/smoke_forecast_truth.py`; honest status is `CODE COMPLETE — WAITING FOR PRODUCTION/DEVICE QA` until `api.hiair.io` smoke and physical-device QA.
