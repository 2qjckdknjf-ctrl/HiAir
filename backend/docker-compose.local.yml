services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: hiair
      POSTGRES_PASSWORD: hiair
      POSTGRES_DB: hiair
    ports:
      - "5432:5432"
    volumes:
      - hiair-local-postgres:/var/lib/postgresql/data

volumes:
  hiair-local-postgres:
